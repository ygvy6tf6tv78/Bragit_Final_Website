# Logo Particle Experience

This is a standalone static experience. It is not connected to the Bragit marketing website.

Run locally from this folder:

```bash
python3 -m http.server 8080
```

Then open `http://localhost:8080`.
